# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
dev-dist
*.local

# Test / coverage
coverage
playwright-report
test-results

# Env
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*

# Supabase — kept private (public repo). Keep a LOCAL backup of supabase/migrations;
# that folder is how the DB schema is reproduced. Vercel deploy doesn't need it.
supabase

# TS build info
*.tsbuildinfo

# Lockfile — intentionally excluded by project owner
package-lock.json
